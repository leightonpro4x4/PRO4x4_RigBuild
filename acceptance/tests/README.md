# Stage 1–3 acceptance

Run npm test for the read-only hash/layout gate. Run npm run check for script syntax. Complete WF business suites are recorded in the manifest but not run or integrated at this stage. Historical test verdicts are evidence, not fresh consolidated results.

The test chain also runs catalogue.mjs: all WF2 source fields and null values are compared to deterministic normalization, with exact catalogue/evidence counts, vehicle-aware keys, six identity matches, the explicit Powerboards gap, unapproved Y62 3D base, and the exact Alpha 93 A$10,239 fixture. No dependency engine is executed or integrated.
