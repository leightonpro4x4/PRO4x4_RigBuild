const backend=window.PRO4X4_BACKEND.active(),$=s=>document.querySelector(s);const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));const money=n=>new Intl.NumberFormat('en-AU',{style:'currency',currency:'AUD',maximumFractionDigits:0}).format(Number(n)||0);const fmt=d=>new Date(d).toLocaleString('en-AU',{day:'2-digit',month:'short',year:'numeric',hour:'2-digit',minute:'2-digit'});
function lineTotal(a){return Object.values(a.pricing||{}).reduce((n,v)=>n+(v==null?0:Number(v)||0),0)}
async function render(){const token=new URLSearchParams(location.search).get('token'),host=$('#sharedBuild');let resolved=null;try{resolved=token?await backend.resolveProjectShare(token):null}catch{}if(!resolved){host.innerHTML='<div class="card share-error"><div class="eyebrow">SHARE LINK</div><h1>BUILD LINK UNAVAILABLE.</h1><p>This token is invalid, expired, revoked, or unavailable from the current Rig Builder persistence service.</p></div>';return;}const {share,project,revision}=resolved,x=revision.snapshot,serverBacked=backend.mode!=='local-adapter';host.innerHTML=`<section class="share-hero"><div><div class="eyebrow">PRO4X4 SHARED BUILD · ${esc(share.role)}</div><h1>${esc(project.title)} <span>${esc(revision.id)}</span></h1><p>${esc(x.vehicle?.yearRange||'')} ${esc(x.vehicle?.make)} ${esc(x.vehicle?.model)} ${esc(x.vehicle?.trim||'')} · ${esc(x.vehicle?.paint||'')}</p></div><div class="share-meta"><span>${esc(project.id)}</span><b>${money(x.pricing?.knownSubtotal||0)}</b><small>Known subtotal · not necessarily fitted final</small></div></section><div class="local-share-warning">${serverBacked?'Server-backed read-only snapshot. This token is pinned to one immutable project revision.':'Browser-local fallback preview. This token only resolves in the browser profile that created it.'}</div><div class="share-grid"><section class="card share-items"><div class="section-head"><div><div class="eyebrow">BUILD SPEC</div><h2 style="margin:0">Selected gear</h2></div><span class="mini-state">${x.selections?.length||0} ITEMS</span></div>${(x.selections||[]).map(a=>`<div class="shared-item"><div><b>${esc(a.name)}</b><span>${esc(a.brand)} · ${esc(a.sku)}</span></div><b>${money(lineTotal(a))}</b></div>`).join('')}</section><aside class="card share-summary"><div class="eyebrow">PROJECT SNAPSHOT</div><h2>${esc(x.lead?.name||'Customer build')}</h2><div class="staff-line"><span>Revision</span><b>${esc(revision.id)} · checksum ${esc(revision.checksum)}</b></div><div class="staff-line"><span>Saved</span><b>${fmt(revision.createdAt)}</b></div><div class="staff-line"><span>Catalogue</span><b>${esc(x.catalogue?.revision||'')}</b></div><div class="staff-line"><span>Workflow</span><b>${esc(x.workflow?.status||'')}</b></div><div class="staff-line"><span>Render</span><b>${x.render?.productionReady?'Production ready':'Assets still pending'}</b></div><div class="staff-line"><span>Known mass</span><b>${x.weight?.knownAccessoryMassKg||0} kg + ${x.weight?.unknownAccessoryMassCount||0} unknown</b></div>${share.role==='staff-review'?`<a class="primary link-button" href="sales.html">OPEN STAFF QUEUE</a><a class="secondary link-button" href="index.html?project=${encodeURIComponent(project.id)}&revision=${encodeURIComponent(revision.id)}">OPEN CONFIGURATOR REVISION</a>`:''}</aside></div>`}
render();
