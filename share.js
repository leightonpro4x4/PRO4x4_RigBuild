const backend=window.PRO4X4_BACKEND.active(),sharedModel=window.PRO4X4_CUSTOMER_SHARED_BUILD,$=s=>document.querySelector(s);const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));const money=n=>new Intl.NumberFormat('en-AU',{style:'currency',currency:'AUD',maximumFractionDigits:0}).format(Number(n)||0);const fmt=d=>d?new Date(d).toLocaleString('en-AU',{day:'2-digit',month:'short',year:'numeric',hour:'2-digit',minute:'2-digit'}):'—';
function lineTotal(a){return Object.values(a.pricing||{}).reduce((n,v)=>n+(v==null?0:Number(v)||0),0)}
function gateLabel(type){return ({'fitment-blocked':'FITMENT BLOCKED','dependency':'REQUIRED PART','dependency-any-of':'SUPPORT CHOICE REQUIRED','vehicle-fitment':'VEHICLE FITMENT','fitment-review':'FITMENT REVIEW'})[type]||String(type||'FITMENT REVIEW').replaceAll('-',' ').toUpperCase()}
function renderGates(model){const rows=model.gates.rows||[],checks=model.setupChecks||[];if(!rows.length&&!checks.length)return '<div class="shared-clear"><b>NO OPEN FITMENT GATES</b><span>This immutable revision records no unresolved dependency, vehicle-fitment or staff-review conditions.</span></div>';const gates=rows.map(g=>`<div class="shared-gate ${g.blocking?'blocking':'review'}"><div class="shared-gate-head"><span>${esc(gateLabel(g.type))}</span><b>${g.blocking?'ACTION REQUIRED':'PRO4X4 REVIEW'}</b></div><p>${esc(g.note)}</p>${g.alternatives?.length?`<small>Verified alternatives: ${g.alternatives.map(a=>esc(a.name)).join(' OR ')}</small>`:''}</div>`).join('');const setup=checks.map(c=>`<div class="shared-gate review"><div class="shared-gate-head"><span>VEHICLE SETUP CHECK</span><b>PRO4X4 REVIEW</b></div><p>${esc(c.productName)}</p><small>${c.conditions.map(esc).join(' · ')}</small></div>`).join('');return gates+setup}
function renderVisual(model){const v=model.visual,layers=v.layers||[];const layerRows=layers.length?layers.map(layer=>`<div class="shared-visual-row ${esc(layer.state)}"><div><span>${esc(layer.layerId)}${layer.exactSku?` · ${esc(layer.exactSku)}`:''}</span><b>${esc(layer.label)}</b></div><p>${esc(layer.detail||'Exact production visual state is recorded on this revision.')}${layer.reason?` <small>Reason: ${esc(layer.reason)}</small>`:''}</p></div>`).join(''):'<div class="shared-clear"><b>NO PRODUCTION LAYERS RECORDED</b><span>This revision does not contain an approved production layer stack. No stand-in imagery is shown.</span></div>';return `<div class="shared-visual-head"><div><div class="eyebrow">EXACT PRODUCTION VISUAL STATE</div><h2>${esc(v.headline||'PRODUCTION VISUAL STATE')}</h2></div><span class="mini-state ${v.productionReady?'confirmed':'engineering'}">${v.productionReady?'READY':'EXACT MATCH PENDING'}</span></div><p class="shared-visual-summary">${esc(v.summary||'No alternate visual state will be substituted.')}</p><div class="shared-rule"><b>${esc(model.customerVisualRule)}</b><span>Exact match required · fallback ${esc(v.fallbackPolicy||'none')} · source ${esc(v.snapshotSource)}</span></div><div class="shared-visual-counts"><span>${v.counts.available} AVAILABLE</span><span>${v.counts.missing} MISSING</span><span>${v.counts.blocked} BLOCKED</span></div>${layerRows}`}
async function render(){const token=new URLSearchParams(location.search).get('token'),host=$('#sharedBuild');let resolved=null;try{resolved=token?await backend.resolveProjectShare(token):null}catch{}if(!resolved){host.innerHTML='<div class="card share-error"><div class="eyebrow">SHARE LINK</div><h1>BUILD LINK UNAVAILABLE.</h1><p>This token is invalid, expired, revoked, or unavailable from the current Rig Builder persistence service.</p></div>';return;}const {share,project,revision}=resolved,x=revision.snapshot,serverBacked=backend.mode!=='local-adapter',model=sharedModel.build(x,{projectId:project.id,revisionId:revision.id,revisionChecksum:revision.checksum,revisionCreatedAt:revision.createdAt,role:share.role});host.innerHTML=`<section class="share-hero"><div><div class="eyebrow">PRO4X4 SHARED BUILD · ${esc(model.role)}</div><h1>${esc(project.title)} <span>${esc(model.revisionId)}</span></h1><p>${esc(model.vehicle)} · immutable shared revision</p></div><div class="share-meta"><span>${esc(model.projectId)}</span><b>${money(model.pricing.knownSubtotal)}</b><small>Known subtotal · not necessarily fitted final</small></div></section><div class="local-share-warning">${serverBacked?'Server-backed read-only snapshot. This token is pinned to one immutable project revision.':'Browser-local fallback preview. This token only resolves in the browser profile that created it.'}</div><div class="share-grid"><div class="share-main"><section class="card share-items"><div class="section-head"><div><div class="eyebrow">BUILD SPEC</div><h2 style="margin:0">Selected gear</h2></div><span class="mini-state">${model.itemCount} ITEMS</span></div>${model.selections.map(a=>`<div class="shared-item"><div><b>${esc(a.name)}</b><span>${esc(a.brand)} · ${esc(a.sku)}</span></div><b>${money(lineTotal(a))}</b></div>`).join('')}</section><section class="card shared-fitment"><div class="section-head"><div><div class="eyebrow">IMMUTABLE FITMENT STATE</div><h2 style="margin:0">Dependencies + checks</h2></div><span class="mini-state ${model.gates.blockingCount?'blocked':model.gates.reviewCount||model.setupChecks.length?'engineering':'confirmed'}">${model.gates.blockingCount?'BLOCKED':model.gates.reviewCount||model.setupChecks.length?'REVIEW':'CLEAR'}</span></div><p class="shared-section-note">These requirements are read from this exact saved revision. This share view does not re-evaluate fitment against a newer catalogue or mutable configurator state.</p>${renderGates(model)}</section><section class="card shared-visual">${renderVisual(model)}</section></div><aside class="card share-summary"><div class="eyebrow">PROJECT SNAPSHOT</div><h2>${esc(x.lead?.name||'Customer build')}</h2><div class="staff-line"><span>Revision</span><b>${esc(model.revisionId)} · checksum ${esc(model.revisionChecksum)}</b></div><div class="staff-line"><span>Saved</span><b>${fmt(model.revisionCreatedAt)}</b></div><div class="staff-line"><span>Catalogue</span><b>${esc(model.catalogueRevision||'')}</b></div><div class="staff-line"><span>Workflow</span><b>${esc(model.workflowStatus||'')}</b></div><div class="staff-line"><span>Fitment gates</span><b>${model.gates.blockingCount} blocking · ${model.gates.reviewCount} review</b></div><div class="staff-line"><span>Render</span><b>${model.visual.productionReady?'Exact production ready':'Exact assets still pending'}</b></div><div class="staff-line"><span>Known mass</span><b>${model.weight?.knownAccessoryMassKg||0} kg + ${model.weight?.unknownAccessoryMassCount||0} unknown</b></div><div class="shared-lock"><b>REVISION LOCKED</b><span>Fitment, BOM and visual-state detail above come only from the revision pinned to this share token.</span></div>${share.role==='staff-review'?`<a class="primary link-button" href="sales.html">OPEN STAFF QUEUE</a><a class="secondary link-button" href="index.html?project=${encodeURIComponent(project.id)}&revision=${encodeURIComponent(revision.id)}">OPEN CONFIGURATOR REVISION</a>`:''}</aside></div>`}
render();
