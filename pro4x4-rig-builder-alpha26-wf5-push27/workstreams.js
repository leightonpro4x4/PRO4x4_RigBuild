const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
if(window.PRO4X4_AUTH?.current?.().role!=='admin')window.PRO4X4_AUTH?.setLocalRole?.('admin','PRO4X4 Admin');
const board=window.PRO4X4_WORKSTREAMS;
if(!board)throw new Error('Workflow board data unavailable');
const statusLabel=s=>({READY:'READY TO ADVANCE',BLOCKED_EXTERNAL_ASSET:'EXTERNAL ASSET BLOCK',GATE:'INTEGRATION GATE'}[s]||s);
const pillClass=s=>s==='READY'?'ok':s==='GATE'?'gate':'blocked';
document.querySelector('#alphaLabel').textContent=board.alpha.toUpperCase();
document.querySelector('#principles').innerHTML=`<div class="eyebrow">LOCKED RULES</div><div class="weight-grid"><div><span>DESIGN</span><b>${esc(board.principles.design)}</b></div><div><span>VISUAL MILESTONE</span><b>${esc(board.principles.visualMilestone)}</b></div><div><span>CATALOGUE MILESTONE</span><b>${esc(board.principles.catalogueMilestone)}</b></div><div><span>RENDER RULE</span><b>${esc(board.principles.renderRule)}</b></div></div>`;
document.querySelector('#workstreamGrid').innerHTML=board.workstreams.map(w=>`<article class="card workstream-card"><div class="workstream-head"><div><div class="eyebrow">${esc(w.id)} · PRIORITY ${w.priority}</div><h2>${esc(w.name)}</h2></div><span class="workstream-status ${pillClass(w.status)}">${esc(statusLabel(w.status))}</span></div><h3>Next package</h3><p>${esc(w.nextPackage)}</p><details><summary>Ownership boundary</summary><div class="workstream-cols"><div><b>OWNS</b><ul>${w.owns.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div><div><b>DOES NOT OWN</b><ul>${w.excludes.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div></div></details><details><summary>Dependencies / definition of done</summary><div class="workstream-cols"><div><b>DEPENDENCIES</b><ul>${w.dependencies.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div><div><b>DONE WHEN</b><ul>${w.doneWhen.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div></div></details></article>`).join('');
document.querySelector('#mergeGate').innerHTML=`<p>${esc(board.mergeGate.branchPolicy)}</p><ul>${board.mergeGate.requiredChecks.map(x=>`<li>${esc(x)}</li>`).join('')}</ul>`;
